# CyberShield AI — frontend environment
# Copy to .env.local and adjust as needed.

# Base URL of the REST API (Node.js + Express + MongoDB backend).
VITE_API_BASE_URL=http://localhost:4000/api

# Set to "false" to disable the mock adapter and hit the real backend above.
VITE_USE_MOCK_API=true
